{
  "pluginAlias": "IecElectricity",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "headerDisplay": "IEC Electricity – Israel Electric Company",
  "footerDisplay": "Log in with your Israeli ID to see meter data in HomeKit",
  "schema": {
    "type": "object",
    "required": ["name"],
    "additionalProperties": false,
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "IEC Electricity"
      },
      "logApiData": {
        "title": "Log API data (development)",
        "type": "boolean",
        "description": "When enabled, logs full JSON from every IEC API call so you can see all available fields for creating new sensors. Turn off after copying what you need.",
        "default": false
      },
      "pollInterval": {
        "title": "Poll Interval (minutes)",
        "type": "integer",
        "description": "How often to fetch data from IEC API (IEC typically updates every 1-2 days)",
        "default": 60,
        "minimum": 15,
        "maximum": 1440
      }
    }
  }
}
